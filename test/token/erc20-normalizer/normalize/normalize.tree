normalize.t.sol
├── when the scalar has not been computed
│  └── it should return the normalized amount
└── when the scalar has been computed
   ├── when the scalar is one
   │  └── it should return the normalized amount
   └── when the scalar is not one
      ├── when the calculation overflows uint256
      │  └── it should revert
      └── when the calculation does not overflow uint256
         ├── when the amount is zero
         │  └── it should return zero
         └── when the amount is not zero
            └── it should return the normalized amount
